@model Smart_Training_Institute_Portal.Models.Enrollment

@{
    ViewData["Title"] = "Edit Enrollment";
}

<div class="page-header d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3">
    <div>
        <h1 class="page-title"><i class="bi bi-pencil-square me-2 text-danger"></i>Edit Enrollment</h1>
        <p class="page-subtitle">Update enrollment status, grades, and course assignments.</p>
    </div>
    <div class="sti-action-bar">
        <a asp-action="Index" class="btn btn-danger">
            <i class="bi bi-arrow-left me-1"></i> Back to List
        </a>
    </div>
</div>

<div class="row justify-content-center">
    <div class="col-lg-10">
        <div class="card sti-card">
            <div class="card-body">
                <form asp-action="Edit">
                    <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                    <div class="row g-3">
                        <div class="col-md-6">
                            <div class="form-floating">
                                <input asp-for="EnrollmentDate" class="form-control" placeholder="Enrollment Date" />
                                <label asp-for="EnrollmentDate" class="fw-semibold"></label>
                            </div>
                            <span asp-validation-for="EnrollmentDate" class="text-danger"></span>
                        </div>
                        <div class="col-md-6">
                            <div class="form-floating">
                                <input asp-for="Status" class="form-control" placeholder="Status" />
                                <label asp-for="Status" class="fw-semibold"></label>
                            </div>
                            <span asp-validation-for="Status" class="text-danger"></span>
                        </div>
                        <div class="col-md-6">
                            <div class="form-floating">
                                <input asp-for="Mark" class="form-control" placeholder="Mark" />
                                <label asp-for="Mark" class="fw-semibold"></label>
                            </div>
                            <span asp-validation-for="Mark" class="text-danger"></span>
                        </div>
                        <div class="col-md-6">
                            <div class="form-floating">
                                <input asp-for="Grade" class="form-control" placeholder="Grade" />
                                <label asp-for="Grade" class="fw-semibold"></label>
                            </div>
                            <span asp-validation-for="Grade" class="text-danger"></span>
                        </div>
                        <div class="col-md-6">
                            <div class="form-floating">
                                <select asp-for="StudentProfileId" class="form-select" asp-items="ViewBag.StudentProfileId"></select>
                                <label asp-for="StudentProfileId" class="fw-semibold"></label>
                            </div>
                            <span asp-validation-for="StudentProfileId" class="text-danger"></span>
                        </div>
                        <div class="col-md-6">
                            <div class="form-floating">
                                <select asp-for="CourseId" class="form-select" asp-items="ViewBag.CourseId"></select>
                                <label asp-for="CourseId" class="fw-semibold"></label>
                            </div>
                            <span asp-validation-for="CourseId" class="text-danger"></span>
                        </div>
                    </div>
                    <input type="hidden" asp-for="Id" />
                    <div class="d-flex justify-content-end gap-2 mt-4">
                        <a asp-action="Index" class="btn btn-danger">
                            <i class="bi bi-arrow-left me-1"></i> Cancel
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-check-circle me-1"></i> Save
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
